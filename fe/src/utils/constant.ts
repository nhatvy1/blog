export enum HttpStatusEnum {
  AUTHORIZATION = 401,
  FORBIDDEN = 403
}

export enum Modules {
  USER = 'user',
  ROLE = 'role'
}

export enum Action {
  CREATE = 'create',
  READ = 'read',
  UPDATE = 'update',
  DELETE = 'delete',
}
